栈:后进先出的数据结构


函数\局部变量\都存在于栈空间中,用完即释放.


是一种特殊的线性表,只允许在一端进行插入或者删除操作的线性表.允许进行插入,删除的操作的一端被叫做栈顶,另一端被叫做栈底.


栈顶的位置时动态的,当栈里没有数据时,称为空栈,栈的插入操作称为进栈或入栈,栈的删除称为退栈或出栈.

压栈:入栈.

栈分为向上增长和向下增长,压栈时地址增加,递增堆栈.压栈时地址减小,叫做递减退栈,linux采用向下增长.


